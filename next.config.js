/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // We can add output: 'export' if we want static export, but Next.js standard SSR/Serverless configuration works perfectly on Vercel
}

module.exports = nextConfig
